// my-theme.ts
import type { DefaultTheme } from 'styled-components'

const theme: DefaultTheme = {
   br: 'var(--br)',
   surface_br: 'var(--sufrace_br)',
   onPrimary: 'var(--onPrimary)',
   primary: 'var(--primary)',
   color: 'var(--color)',
   bg: 'var(--bg)',
}

export default theme
