import styled from 'styled-components'

const StyledPost = styled('div')`
   padding: 1rem 0;

   .post-title {
      margin-bottom: 1rem;
   }
   .post-msg {
      display: flex;
      gap: 1rem;

      margin-bottom: 1rem;
   }
   button {
      display: block;
      margin-left: auto;
   }
`

export default StyledPost
